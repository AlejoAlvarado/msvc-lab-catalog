spring:
  application:
    name: msvc-lab-guest
  config:
    name: ${spring.application.name}
  cloud:
    config:
      uri: http://localhost:8888
  data:
    mongodb:
      uri: mongodb://localhost:27020/countryDb

server:
  port: 8082
